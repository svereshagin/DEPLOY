version: "3.10"

services: 
  django-backend:
    restart: always
    build:
      context: ./studysite # django service folder
    image: studysite
  
  postgresql-db:
    restart: always
    image: postgres
    env_file: ./postgresql-db/.pg-env
